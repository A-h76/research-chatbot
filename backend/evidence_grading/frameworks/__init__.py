"""The four registered framework graders (GRADE, Oxford CEBM, NIH, SIGN)
— see each module's own docstring. frameworks/usptf.py from the task's
own directory tree is deliberately not built (see package docstring)."""
