[tool.black]
line-length = 120
target-version = ['py311']
extend-exclude = '''
/(
  migrations
  | frontend
  | node_modules
)/
'''

[tool.isort]
profile = "black"
line_length = 120
skip = ["migrations", "frontend", "node_modules"]
